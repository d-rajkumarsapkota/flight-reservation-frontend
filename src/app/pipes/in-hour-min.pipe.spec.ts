import { InHourMinPipe } from './in-hour-min.pipe';

describe('InHourMinPipe', () => {
  it('create an instance', () => {
    const pipe = new InHourMinPipe();
    expect(pipe).toBeTruthy();
  });
});
